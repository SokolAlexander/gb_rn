import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  todosContainer: {
    padding: 16,

    backgroundColor: 'white',
  },
  sectionHeader: {
    marginVertical: 15,
    fontSize: 20,
  },
});
