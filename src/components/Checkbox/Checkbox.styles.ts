import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    width: 60,
    height: 60,
    borderRadius: 5,
    borderWidth: 2,
    borderColor: '#453489',
  },
  inner: {
    height: '100%',
    width: '100%',
    backgroundColor: '#453489',
  },
});
